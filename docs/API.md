# API.md

Este archivo se genera/actualiza automáticamente a partir del documento OpenAPI 3.1 expuesto por
`apps/api` (`@nestjs/swagger`, ver `docs/PLAN.md` sección 0.4 y 12). Se completa a partir de F2
(cuando exista un servidor Nest arrancable con rutas reales) y se exporta en CI (sección 12).

Pendiente: primer export tras F2.
